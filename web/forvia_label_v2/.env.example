# 复制为 .env 后按需修改。
#
# FORVIA_DATA_DIR：宿主机上 TDMS / 数据库所在的根目录，会挂载到容器内的 /data。
# 之后在任务表单里填“容器内路径”，即 /data/... 开头。
#
# 例（默认，相对当前目录）：
FORVIA_DATA_DIR=./data
#
# 例（Mac 外接盘，把整块盘挂进去）：
# FORVIA_DATA_DIR=/Volumes/18555440521
#   → 表单填： TDMS 目录 /data/fault/data_root/factory_raw
#             数据库文件夹 /data/fault/data_root/metadata
#
# 例（本机某文件夹）：
# FORVIA_DATA_DIR=/Users/yourname/forvia_data
